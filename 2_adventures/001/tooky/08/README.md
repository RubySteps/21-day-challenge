* Added a very simple suite abstraction today,
* To make this easier I needed to remove the `*tests` API for `run_all`

## Tomorrow

I think tests and suites are going to need names... Maybe that's next.
