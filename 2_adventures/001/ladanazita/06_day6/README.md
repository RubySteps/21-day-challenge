#Day 6
Did not get to finish a challenge but reading about .map. Attempting to write a method that accepts a list and block, runs the block for each item adn returns a new array with the block return values
