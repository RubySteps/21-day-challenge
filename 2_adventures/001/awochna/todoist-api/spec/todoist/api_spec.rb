require 'spec_helper'

describe Todoist::Api do
  it 'has a version number' do
    expect(Todoist::Api::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
